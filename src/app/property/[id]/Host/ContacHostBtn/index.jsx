import styles from './index.module.css';

export const ContacHostBtn = () => {
  return (
    <button className={`${styles.contact_host_btn}`}>Contact Host</button>
  )
}

export default ContacHostBtn